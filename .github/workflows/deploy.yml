name: Deploy to Ubuntu Server

on:
  push:
    branches:
      - main

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Deploy to server
        uses: appleboy/ssh-action@master
        with:
          host: 68.66.251.93
          username: root
          password: d9de659eeac2dfde
          port : 7822
          script: |
            mkdir /cicd/pipHunterCiCD
            cd /cicd/pipHunterCiCD
            git clone https://github.com/mohamadTamimi123/pipHunterCiCd.git
            docker stop $(docker ps -a -q)
            docker rm $(docker ps -a -q)
            docker-compose up -d
            cd /cicd/pipHunterCiCD/pipHunterCiCD/backend/backend-app
            npm install
            npm run start:dev
           
